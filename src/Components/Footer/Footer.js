import React from 'react';
import './Footer.css';

const Footer = ({title}) => {
    return (
        <div>
            <h1 className='Footer-title'>{title}</h1>
        </div>
    )
}

export default Footer;